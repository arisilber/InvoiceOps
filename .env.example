# -----------------------------------------------------------------------------
# Local development (plain Postgres)
# -----------------------------------------------------------------------------
# 1. Install & start Postgres (e.g. Homebrew: brew install postgresql@16 && brew services start postgresql@16)
# 2. Create DB: createdb invoiceops  (or: psql -c "CREATE DATABASE invoiceops;")
# 3. Leave DATABASE_URL commented out so the app uses DB_* below
# 4. Run migrations: npm run migrate
# 5. Start server: npm run server

# DATABASE_URL=

DB_HOST=localhost
DB_PORT=5432
DB_NAME=invoiceops
DB_USER=
DB_PASSWORD=
DB_SSL=false

# -----------------------------------------------------------------------------
# Production / Supabase
# -----------------------------------------------------------------------------
# Uncomment and set DATABASE_URL; the app will use it and ignore DB_* above.
# Use Session mode connection string if direct connection fails (IPv6/DNS).
#
# DATABASE_URL=postgresql://postgres:PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres
# DB_SSL=true

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
PORT=4567
NODE_ENV=development

# Required for auth (use strong secrets in production)
JWT_SECRET=your-secret-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production
